# WorldGuard migration to Pinac 26.2

## Baseline review (before implementation, 2026-09-08)

- Ownership: this repository only; consumers and server remain read-only. No AGENTS.md found in repository or immediate parent locations.
- Clean starting branch `version/7.0.x`, HEAD `2dbd1fd06691d920f050043a4beca6fb55d75330`, tracking `origin/version/7.0.x`. Origin `https://github.com/MasivoSMP/WorldGuard.git`; remote HEAD confirmed `version/7.0.x`. Fetched origin, verified no local/remote dev branch, successfully ran `git pull --ff-only origin version/7.0.x`, then `git switch -c dev-26.2`. No unrelated changes.
- Read-only upstream fetched from `https://github.com/EngineHub/WorldGuard.git`; `upstream/version/7.0.x` is `7c280a13`. Its commits `ab33d3fd`, `7c38af4c`, `f0239b63`, `31c9320d`, `c90c4838` provide Java25/26.2 build, pig-zap, hostile hierarchy and material/sulfur protection evidence. Preserve fork ancestry; selectively port necessary changes rather than merge all upstream features.
- Baseline build: Gradle8.12, Java21, Paper1.21.11, WorldEdit7.3.18, WG7.0.18-SNAPSHOT. Target: Java25 and actual `io.canvasmc.pinac:pinac-api:26.2-local` from accepted server implementation `36efa7fe8c630737216393ca92cf14f1db2c759b`. API expected SHA256 `1952d473fb3a4b57a227962759ac8776b8e28eacda000196ef62609f8239b4ee`. No compiled NMS imports; optional Forge fake-player reflection in InteropUtils derives CraftPlayer from server package and remains unchanged. No bundle or Horizon/Velocity dependency needed.
- Required provider: official WorldEdit7.4.4, as pinned by upstream26.2 WG; release https://github.com/EngineHub/WorldEdit/releases/tag/7.4.4 and EngineHub Maven artifacts. No local provider source edits planned. Runtime provider installation still requires validation.

## Applicable flow / preservation

- `Entities.isHostile` feeds fishing, entity use, damage and mob flags in EventAbstractionListener/RegionProtectionListener. Existing Slime hierarchy loses magma cubes; port upstream Enemy interface once in shared helper. Ground-slime spawn rule explicitly targets EntityType.SLIME and must remain slime-only.
- WorldGuardEntityListener pig-zombification configuration currently uses legacy PigZapEvent; port upstream Paper EntityZapEvent listener and its shared cancellation handler, retaining Spigot fallback. Existing HIGH priority / ignoreCancelled and disable-pig-zombification setting retained.
- Materials classifications feed interaction/build checks and spawn eggs; upstream26.2 additions and sulfur-cube cause tracking preserve protection for new target mechanics. EventAbstractionListener -> Cause -> RegionProtectionListener/RegionQuery -> registered flags remains the rule path. Do not add optional lava-harden flag, change natural-spawn timing or bring unrelated optimization/command changes.
- Masivo localization (`fc4c4d2d`, MessageService, defaults/lang.yml, deny-message keys) and all existing ownership guards (`2dbd1fd0`, PaperInterop and projectile/hanging/damage paths) remain intact. Folia scheduler selection in plugin/session/container/move/vehicle code is retained; cross-region runtime behavior is a gate.
- WG uses WorldEdit's own formatting ClickEvent, not Adventure ClickEvent. Upgrade WorldEdit dependency and compile its consumers; do not rewrite text APIs speculatively. No BookMeta builder or bed block-entity PDC storage found.
- Region data uses plugin-owned `getWorldsDataFolder()/world-name/regions.yml` through DirectoryYamlDriver/YamlRegionFile or existing SQL driver. No raw vanilla playerdata/level.dat readers. Preserve world-name identity, region/flag keys, storage schemas, UUID domains, membership, priorities, inheritance, bypass and save lifecycle. Clock use found only diagnostic getFullTime; no gameplay clock rewrite needed.
- Read-only consumers: HMCCosmetics registers/queries WG flags; MasivoXLands bridge uses region container/query/manager and registered lands-claim flag. Public contracts and serialized keys must remain unchanged; consumer owners validate their own compilation/runtime.

## Planned validation

Run the actual target build and existing tests/checkstyle/license checks, plus a focused existing-JUnit regression for changed entity classification. Inspect resolved compile/runtime dependencies and shaded JAR for stale/bundled server APIs and retained Masivo classes/resources. Record commands, failures, final artifact paths/hashes and remote commit checkpoints below. No shared Maven publication, deployment, server changes, production worlds or external services. Runtime loading, permissions/region cancellation, sulfur interactions, pig lightning, cross-region ownership, old region YAML/SQL and HMCCosmetics/lands integration remain NOT RUN until exercised in an isolated accepted Pinac environment.

## Implementation and development validation

- Ported upstream compatibility hunks from `7c38af4c`, `f0239b63`, `ab33d3fd`/`31c9320d` (materials), and `c90c4838`. The sulfur block-break hunk was adapted to keep Masivo's `use-dynamite` localization key; all original ownership guards are retained. No region/config/SQL storage implementation changed.
- Initial minimal Gradle9/Java25 conversion failed because `org.cadixdev.licenser:0.6.1` threw StackOverflowError. Adopted upstream's Crankcase0.1.0 build conventions, Gradle9.6.1 and Shadow9.3.2 (including upstream Shadow publication fix). Retained WG version7.0.18-SNAPSHOT, artifact names, `-dist` shading/minimization, disabled `-Werror`, and existing `-PgitCommitHash` override in the internal-version provider. No publish task run.
- Kept explicit EngineHub repositories plus Canvas and Maven-local scoped to `io.canvasmc.pinac`. Upstream repository-reconfiguration plugin rejected Canvas, so it is not applied. WorldEdit's jlibnoise POM requires the existing `maven.enginehub.org/repo` endpoint (scoped to com.sk89q.lib); the newer repository supplied its JAR but not its POM. No fake artifacts, dependency exclusions or stub substitutions.
- Crankcase header verification exposed 21 pre-existing mixed-line-ending working files (Git index was LF). Normalized those checkout bytes to LF without logical/source diffs; kept verification enabled.
- PASS: `JAVA_HOME=C:/Users/CHESV/.gradle/jdks/eclipse_adoptium-25-amd64-windows.2; ./gradlew.bat build --console=plain` (51s, 35 tasks). Build includes Java compilation, Javadocs, shadow JAR, verifyHeader, Checkstyle main/test, and tests. 183 tests, 0 failures/errors/skips: 181 existing core tests including all four MessageService tests, plus two new Bukkit EntitiesTest tests using real target interfaces and minimal proxies. Tests cover magma/slime/ghast/dragon/shulker hostile classification, passive sulfur cubes, sulfur TNT classification with/without explosive state, and explosion flag routing. Warnings from pre-existing deprecated APIs/Error Prone remain nonfatal under preserved suppression policy.
- PASS: `./gradlew.bat :worldguard-bukkit:dependencies --configuration compileClasspath --console=plain` and corresponding `runtimeClasspath`. Compile resolves only Pinac26.2-local as server API, WorldEdit core/bukkit/libs7.4.4, Adventure5.2.0, Guava33.6.0-jre, Gson2.14.0, SnakeYAML2.3. No old Paper/Spigot/Canvas API. Runtime graph intentionally omits compile-only Pinac; required WorldEdit remains external.
- PASS: shaded JAR inspection: 825 entries; no bundled Bukkit/Paper/Canvas/NMS/Adventure/WorldEdit classes. Masivo MessageService, PaperInterop, defaults/lang.yml, and Paper pig-zap listener present. plugin.yml retains WorldEdit dependency and folia-supported=true, with api-version26.2. Exact post-commit artifact hashes follow below.
- Logs: `.gradle/migration-26.2/build.log`, `compile-dependencies.log`, `runtime-dependencies.log` (local ignored). Test XML: `worldguard-core/build/test-results/test/`, `worldguard-bukkit/build/test-results/test/`. No Maven-local publication or consumer files changed.
- Source evidence confirms accepted Pinac `ServerBuildInfoImpl.java.patch` advertises BRAND_FOLIA_ID compatibility, preserving the existing isFolia selection. Actual startup/thread ownership and optional Forge reflection are not runtime-tested.

## Runtime gates / limits

NOT RUN: accepted Pinac boot with official compatible WorldEdit runtime, permissions/bypass/member/group/inheritance rules in copied old regions, world load/unload/save/restart, SQL persistence, pig lightning cancellation, fishing/entity protection for magma/slime, sulfur cube interaction/shearing/player attribution/TNT flag/explosion cancellation, projectile/hanging/invincibility ownership and asynchronous teleports, translated deny messages/click interactions, and HMCCosmetics/lands flag registration/query/save integration. No isolated deployed stack was provided or started; no production/shared services, data, worlds, server files, deployment or releases were touched. Successful compilation/tests are development acceptance only.

Observed remaining limitations: optional runtime integrations and region-thread behavior have no executable server validation here. Inferred risks: target mechanics may exercise event ordering/ownership only seen in live play; WorldEdit7.4.4 is the official upstream compile pin, not a verified installed production build. Existing world-name storage identity and custom flag keys are deliberately unchanged.
